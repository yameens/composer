educational tool to learn the theory behind chords, with your hands!

Type:
  • Georgia (or assets/Georgia.ttf) for circles, top HUD, instrument buttons, hand labels.
  • Jacquard 24 (assets/Jacquard24-Regular.ttf) for bottom “COMPOSER” wordmark + Q/T/V/S key row only.
  Fallbacks: Calibri / Fira Mono (see ui_circles._resolve_body_font_path).

Wordmark: optional assets/conductor_brand.png; else vector COMPOSER in Jacquard.
  Optional PNG from TeX:  python3 tools/export_conductor_brand.py  (TeX Live + duerer + pdftoppm).
